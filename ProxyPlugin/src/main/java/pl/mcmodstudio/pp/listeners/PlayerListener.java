package pl.mcmodstudio.pp.listeners;

import org.bukkit.event.Listener;

public class PlayerListener implements Listener {
    // Tutaj w przyszłości możesz dodać eventy, np. @EventHandler na wejście gracza
}